# ── Dữ liệu của BTC: KHÔNG BAO GIỜ commit ────────────────────────────
# Đẩy dữ liệu BTC lên GitHub là phát tán ra ngoài phạm vi cuộc thi.
/data/
!data/.gitkeep
*.json
*.jsonl
!configs/*.json
!docs/*.json
!tests/fixtures/*.json
!vendor/btc_scoring/*.json

# ── Trọng số & artefact nặng ─────────────────────────────────────────
checkpoints/
!checkpoints/.gitkeep
*.npy
*.npz
*.pt
*.bin
*.safetensors
*.faiss
*.pkl

# ── Kết quả chạy ─────────────────────────────────────────────────────
runs/
outputs/*
!outputs/label_audit/
submission*.zip
*.log
docs/exclusion_decisions.json

# ── Python ───────────────────────────────────────────────────────────
__pycache__/
*.py[cod]
.venv/
venv/
.ipynb_checkpoints/
.pytest_cache/
.DS_Store


outputs/label_audit/*.json
outputs/label_audit/packet.html
outputs/label_audit/manual_audit.csv
!outputs/label_audit/manual_audit_r*.csv