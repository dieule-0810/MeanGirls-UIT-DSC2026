"""Tầng dữ liệu (P2): parse_corpus, chunker, split_data.

File rỗng nhưng bắt buộc: thiếu nó, `src.data` chỉ chạy được nhờ namespace package —
may mắn chứ không phải thiết kế, và hỏng ngay khi chạy từ thư mục khác hoặc khi đóng gói.
"""
